# Secrets — NEVER commit your API key
.env
server/.env
web/.env.local

# Node dependencies (re-created with "pnpm install")
node_modules/

# Build output (re-created with "pnpm build")
dist/

# Compiled C output (re-created by compiling)
*.exe
*.o

# Deliverables: QA render artifacts (regeneratable from the .js generators)
deliverables/qa/

# OS junk
.DS_Store
Thumbs.db
